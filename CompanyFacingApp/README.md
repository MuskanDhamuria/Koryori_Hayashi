
  # Koryori Hayashi Tracking 

  ## Running the code

  Run `cd CompanyFacingApp` to install the dependencies.

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  